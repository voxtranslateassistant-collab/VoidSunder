import { NextResponse } from "next/server";
import { deleteProviderKey, listStoredProviderIds, saveProviderKey } from "@/lib/llm/provider-keys";
export const runtime="nodejs"; export const dynamic="force-dynamic";
export async function GET(){try{return NextResponse.json({providers:await listStoredProviderIds()});}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Não foi possível carregar."},{status:401});}}
export async function POST(request:Request){try{const body=await request.json();const provider=String(body.provider??"");const apiKey=String(body.apiKey??"");const check=await verify(provider,apiKey);if(!check.ok)return NextResponse.json({error:check.message},{status:400});await saveProviderKey(provider,apiKey);return NextResponse.json({ok:true,message:check.message});}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Não foi possível salvar."},{status:400});}}
export async function DELETE(request:Request){try{await deleteProviderKey(new URL(request.url).searchParams.get("provider")??"");return NextResponse.json({ok:true});}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Não foi possível remover."},{status:400});}}
async function verify(provider:string,key:string){if(key.trim().length<12)return{ok:false,message:"A chave informada parece inválida."};const endpoints:Record<string,string>={openai:"https://api.openai.com/v1/models",groq:"https://api.groq.com/openai/v1/models",openrouter:"https://openrouter.ai/api/v1/auth/key",gemini:`https://generativelanguage.googleapis.com/v1beta/models?key=${encodeURIComponent(key.trim())}`};const url=endpoints[provider];if(!url)return{ok:true,message:"Chave guardada com sucesso e criptografada. Este provedor ainda aguarda adaptador para uso no Lab de IA."};try{const r=await fetch(url,{headers:provider==="gemini"?{}:{Authorization:`Bearer ${key.trim()}`},signal:AbortSignal.timeout(10000)});return r.ok?{ok:true,message:"Chave validada, criptografada e pronta para uso na plataforma."}:{ok:false,message:"Não foi possível validar a chave. Confira permissões e limite da conta."};}catch{return{ok:false,message:"Não foi possível alcançar o provedor para validar a chave agora."};}}
