"use client";
import { useState } from "react";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
const providers = ["gemini", "groq", "openrouter"];
export function ProviderKeyManager() { const [provider, setProvider] = useState("gemini"); const [apiKey, setApiKey] = useState(""); const [message, setMessage] = useState(""); async function save() { const r = await fetch("/api/llm/providers", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ provider, apiKey }) }); const data = await r.json(); setMessage(r.ok ? "Chave salva com criptografia no servidor." : data.error ?? "Não foi possível salvar."); if (r.ok) setApiKey(""); } return <div className="flex flex-col gap-3 border border-ash-border bg-surface-1 p-5"><label className="text-micro-caps text-graphite-veil">Provedor</label><select value={provider} onChange={(e) => setProvider(e.target.value)} className="h-9 border border-ash-border bg-surface-2 px-3 text-sm"><option value="gemini">Google Gemini</option><option value="groq">Groq</option><option value="openrouter">OpenRouter</option></select><Input type="password" value={apiKey} onChange={(e) => setApiKey(e.target.value)} placeholder="Cole a chave API" autoComplete="off" /><Button type="button" variant="primary" disabled={!apiKey.trim()} onClick={save}>Salvar chave</Button>{message && <p className="text-xs text-fog-blue">{message}</p>}</div>; }
