"use client";
import { useState } from "react";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
const providers = [["gemini","Google Gemini"],["groq","Groq"],["openrouter","OpenRouter"],["openai","OpenAI"],["anthropic","Anthropic"],["together","Together AI"],["cohere","Cohere"],["huggingface","Hugging Face"],["fireworks","Fireworks"],["replicate","Replicate"],["aleph_alpha","Aleph Alpha"],["scale","Scale AI"]];
export function ProviderKeyManager() { const [provider, setProvider] = useState("gemini"); const [custom, setCustom] = useState(""); const [apiKey, setApiKey] = useState(""); const [message, setMessage] = useState(""); async function save() { const id = provider === "custom" ? custom.trim().toLowerCase().replace(/\s+/g,"_") : provider; const r = await fetch("/api/llm/providers", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ provider:id, apiKey }) }); const data = await r.json(); setMessage(r.ok ? "Chave salva com criptografia no servidor." : data.error ?? "Não foi possível salvar."); if (r.ok) setApiKey(""); } return <div className="flex flex-col gap-3 border border-ash-border bg-surface-1 p-5"><label className="text-micro-caps text-graphite-veil">Provedor</label><select value={provider} onChange={(e) => setProvider(e.target.value)} className="h-9 border border-ash-border bg-surface-2 px-3 text-sm">{providers.map(([id,label]) => <option key={id} value={id}>{label}</option>)}<option value="custom">Outro provedor</option></select>{provider === "custom" && <Input value={custom} onChange={(e) => setCustom(e.target.value)} placeholder="Nome ou identificador do provedor" autoComplete="off" />}<Input type="password" value={apiKey} onChange={(e) => setApiKey(e.target.value)} placeholder="Cole a chave API" autoComplete="off" /><Button type="button" variant="primary" disabled={!apiKey.trim() || (provider === "custom" && !custom.trim())} onClick={save}>Salvar chave</Button><p className="text-xs text-graphite-veil">Gemini, Groq e OpenRouter já possuem adaptador no Lab de IA. As demais chaves ficam protegidas e prontas para ativação.</p>{message && <p className="text-xs text-fog-blue">{message}</p>}</div>; }
